<h1>All Tags</h1>

<ul id = "tags">
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name.capitalize, tag_path(tag), class: 'tag_title', id: "tag_#{tag.id}" %>
      <%= link_to "-Delete", tag_path(tag), method: :delete, data: {confirm: "Are you sure?"} %>
    </li>
  <% end %>
</ul>